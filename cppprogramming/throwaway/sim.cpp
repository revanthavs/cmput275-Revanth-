#include <iostream>
#include <vector>
using namespace std;

int main(){
	vector<char> line;
	char letter;
	while (cin >> letter){
		if ((letter == "<") || (letter == "[") || (letter == "]")){
			if (letter == "<"){
				if ()
			}
			else if (letter == "[")
		}
	}
}